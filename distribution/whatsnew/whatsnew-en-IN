Bug fix: VIP Contacts Only now silences non-VIP callers instead of blocking them outright, matching what the screen says it does. Removed auto-blocking based on call frequency or repeated rejections — the app no longer takes automatic blocking action without your explicit rule, since repeat calls can be a real emergency.
